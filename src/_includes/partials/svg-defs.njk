{#- SVG symbol definitions. Included once from base.njk as the first child of
    <body> so that any partial in the layout chain (header.njk, footer.njk,
    etc.) can safely reference symbols via <use href="#…"/> without relying on
    DOM-order between partials. -#}
<svg class="svg-defs" width="0" height="0" aria-hidden="true">
  <defs>
    <symbol id="logo-wordmark" viewBox="0 0 360 60">
      <text x="0" y="46" class="bruna">BRUNA</text>
      <text x="170" y="46" class="pett">þéttingar</text>
    </symbol>
  </defs>
</svg>
