---
layout: layouts/page.njk
pagination:
  data: articles
  size: 1
  alias: article
permalink: "/en/greinar/{{ article.slug }}/"
eleventyComputed:
  title: "{{ article.title.en }}"
  description: "{{ article.summary.en }}"
ogType: "article"
ogImage: "{{ article.image }}"
---
<section class="page-hero page-hero--article">
  <div class="bg" style="background-image: url('{{ article.image }}');"></div>
  <div class="scrim"></div>
  <div class="container">
    <div class="crumbs"><a href="/en/">Home</a> &nbsp;/&nbsp; <a href="/en/greinar/">Articles</a> &nbsp;/&nbsp; <span>{{ article.category[lang] }}</span></div>
    <h1>{{ article.title[lang] }}</h1>
    <p class="kicker">{{ article.summary[lang] }}</p>
  </div>
</section>

<section class="article-body">
  <div class="container">
    <div class="content-prose">
      <div class="article-meta">
        <span>{{ article.date | dateDisplay(lang) }}</span>
        <span>{{ article.readTimeMinutes }} min read</span>
        <span>by {{ article.author }}</span>
      </div>
      <p><em>The full article body is not yet authored. This stub exists so the URL works and the article appears on listings; replace with real content when ready.</em></p>
    </div>
  </div>
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "{{ article.title[lang] | jsonEscape }}",
  "description": "{{ article.summary[lang] | jsonEscape }}",
  "datePublished": "{{ article.date | dateIso }}",
  "author": { "@type": "Person", "name": "{{ article.author | jsonEscape }}" },
  "publisher": { "@type": "Organization", "name": "{{ meta.byLocale[lang].title | jsonEscape }}" },
  "image": "{{ meta.url }}{{ article.image }}",
  "inLanguage": "{{ lang }}"
}
</script>

<section class="cta-band">
  <div class="container">
    <div class="row">
      <h2>Ready to start your project? <span class="ul">Let's talk.</span></h2>
      <div class="actions">
        <a class="btn primary" href="/en/verdreiknir/"><span>Get a quote</span> <span class="arrow">→</span></a>
        <a class="btn ghost" href="/en/greinar/"><span>More articles</span> <span class="arrow">→</span></a>
      </div>
    </div>
  </div>
</section>
